<Window x:Class="RfqInstaller.Uninstall.CredentialsViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stored Passwords" Height="380" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F3F3F3">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Stored Passwords" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#5F5F5F"
                   Text="These are read directly from wherever they're actually stored on this machine. Nothing here is written anywhere."/>

        <StackPanel Grid.Row="2" x:Name="RowsPanel" Margin="0,20,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="100" Height="32" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="RfqInstaller.Demo.Dialogs.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RFQ Application Setup (Demo)"
        Icon="/Assets/scint.png"
        Width="460"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="Segoe UI Variable Text, Segoe UI">
    <Border BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource TitleBarBrush}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="RFQ Application Setup (Demo)" Margin="16,0,0,0"
                           VerticalAlignment="Center" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <Button Grid.Column="1" Content="&#xE8BB;" IsCancel="True"
                        Style="{StaticResource CaptionCloseButtonStyle}"
                        Click="Dismiss_Click" ToolTip="Close"/>
            </Grid>

            <StackPanel Grid.Row="1" Margin="28,24,28,8">
                <TextBlock x:Name="HeadingText" FontSize="20" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                <TextBlock x:Name="MessageText" Style="{StaticResource PageSubtitleStyle}"/>
            </StackPanel>

            <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,1,0,0"
                    Padding="28,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="SecondaryButton" Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,10,0" Click="Secondary_Click"/>
                    <Button x:Name="PrimaryButton" Style="{StaticResource PrimaryButtonStyle}"
                            Click="Primary_Click" IsDefault="True"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

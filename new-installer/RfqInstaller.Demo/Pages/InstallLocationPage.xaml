<UserControl x:Class="RfqInstaller.Demo.Pages.InstallLocationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="600">
    <StackPanel MaxWidth="560" HorizontalAlignment="Left">
        <TextBlock Text="Choose install location" Style="{StaticResource PageTitleStyle}"/>
        <TextBlock Style="{StaticResource PageSubtitleStyle}">
            RFQ Application will be installed into the folder below. Choose a different folder
            if you'd like, or continue with the recommended default.
        </TextBlock>

        <TextBlock Text="Install folder" Style="{StaticResource FieldLabelStyle}" Margin="0,32,0,6"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathTextBox" Grid.Column="0" Style="{StaticResource ModernTextBoxStyle}"
                     TextChanged="PathTextBox_TextChanged"/>
            <Button Grid.Column="1" Content="Browse..." Margin="10,0,0,0"
                    Style="{StaticResource SecondaryButtonStyle}" Click="Browse_Click"/>
        </Grid>
        <TextBlock x:Name="ErrorText" Text="Please choose an install folder." Foreground="#C42B1C"
                   FontSize="12" Margin="0,8,0,0" Visibility="Collapsed"/>

        <TextBlock Style="{StaticResource PageSubtitleStyle}" Margin="0,20,0,0">
            Approximately 250 MB of free disk space is required.
        </TextBlock>
    </StackPanel>
</UserControl>

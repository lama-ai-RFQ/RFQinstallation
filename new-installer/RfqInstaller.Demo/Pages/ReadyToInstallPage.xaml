<UserControl x:Class="RfqInstaller.Demo.Pages.ReadyToInstallPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="600">
    <StackPanel MaxWidth="560" HorizontalAlignment="Left">
        <TextBlock Text="Ready to install" Style="{StaticResource PageTitleStyle}"/>
        <TextBlock Style="{StaticResource PageSubtitleStyle}">
            Review your choices below. Click Install to begin setting up RFQ Application.
        </TextBlock>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0,28,0,0">
            <StackPanel x:Name="SummaryPanel">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="License key" Width="160" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="LicenseKeySummary" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Run mode" Width="160" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="ModeSummary" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Install folder" Width="160" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="PathSummary" FontWeight="SemiBold" TextWrapping="Wrap"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <StackPanel x:Name="ShortcutRow" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Desktop shortcut" Width="160" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="ShortcutSummary" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="AI model" Width="160" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="ModelSummary" FontWeight="SemiBold" TextWrapping="Wrap"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Service account" Width="160" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="ServiceAccountSummary" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
